# {{projectName}}

基于 React、Vite、Wouter、Zustand 和 Axios 的业务应用基础模板。

```bash
npm install
npm run dev
```

## 可用命令

- `npm run dev`：启动开发服务器。
- `npm run build`：构建生产版本。
- `npm test`：运行单元测试。
- `npm run format:check`：检查代码格式。
- `npm run format`：格式化源码。

复制 `.env.example` 为 `.env` 后可调整接口基础地址。
