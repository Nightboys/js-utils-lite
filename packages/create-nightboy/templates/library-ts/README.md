# {{projectName}}

可发布的 TypeScript 工具库基础模板，同时输出 ESM、CommonJS 和类型声明。

```bash
npm install
npm test
npm run build
```

## 可用命令

- `npm run dev`：监听源码并持续构建。
- `npm run build`：生成双模块产物和类型声明。
- `npm test`：运行单元测试。
- `npm run format:check`：检查代码格式。
- `npm run pack:check`：检查 npm 发布包内容。

发布前请补充 `description`、仓库地址、作者信息，并确认 npm 包名可用。
